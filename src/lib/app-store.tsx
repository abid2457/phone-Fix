import { createContext, useContext, useMemo, useState, type ReactNode } from 'react';
import { customers as seedCustomers, jobs as seedJobs, notifications as seedNotifications, parts as seedParts, technicians as seedTechnicians } from './mock-data';
import type { Customer, Job, JobStatus, Part, Technician } from './types';
type Store={jobs:Job[];customers:Customer[];technicians:Technician[];parts:Part[];notifications:typeof seedNotifications; addJob:(job:Job)=>void;updateJob:(id:string,patch:Partial<Job>)=>void;deleteJob:(id:string)=>void;addCustomer:(c:Customer)=>void;addTechnician:(t:Technician)=>void;addPart:(p:Part)=>void;markRead:(id:string)=>void;markAllRead:()=>void};
const Context=createContext<Store|undefined>(undefined);
export function AppStore({children}:{children:ReactNode}){const [jobs,setJobs]=useState(seedJobs);const [customers,setCustomers]=useState(seedCustomers);const [technicians,setTechnicians]=useState(seedTechnicians);const [parts,setParts]=useState(seedParts);const [notifications,setNotifications]=useState(seedNotifications); const value=useMemo(()=>({jobs,customers,technicians,parts,notifications,addJob:(job:Job)=>setJobs(v=>[job,...v]),updateJob:(id:string,patch:Partial<Job>)=>setJobs(v=>v.map(j=>j.id===id?{...j,...patch}:j)),deleteJob:(id:string)=>setJobs(v=>v.filter(j=>j.id!==id)),addCustomer:(c:Customer)=>setCustomers(v=>[c,...v]),addTechnician:(t:Technician)=>setTechnicians(v=>[t,...v]),addPart:(p:Part)=>setParts(v=>[p,...v]),markRead:(id:string)=>setNotifications(v=>v.map(n=>n.id===id?{...n,read:true}:n)),markAllRead:()=>setNotifications(v=>v.map(n=>({...n,read:true}))) }),[jobs,customers,technicians,parts,notifications]);return <Context.Provider value={value}>{children}</Context.Provider>}
export function useAppStore(){const value=useContext(Context);if(!value)throw new Error('AppStore missing');return value}
export const statusOptions:JobStatus[]=['Received','Diagnosis','Estimate','Waiting Approval','Waiting Parts','Repairing','Quality Check','Ready for Collection','Delivered','Cancelled'];
