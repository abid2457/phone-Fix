import { Link, useLocation, useNavigate } from '@tanstack/react-router';
import { BarChart3,Bell,ChevronDown,CircleHelp,ClipboardList,CreditCard,LayoutDashboard,Menu,MessageSquareText,Moon,Package,PanelLeftClose,PanelLeftOpen,Plus,Search,Settings,Smartphone,Store,Sun,Users,Wrench,X } from 'lucide-react';
import { useEffect,useState } from 'react';
import { Dialog,DialogContent,DialogTitle } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';
import { useAppStore } from '@/lib/app-store';
import { cn } from '@/lib/utils';
const groups=[{label:'Navigation',items:[['Overview','/',LayoutDashboard],['Jobs','/jobs',ClipboardList],['Customers','/customers',Users],['Technicians','/technicians',Wrench],['Inventory','/inventory',Package],['Billing','/billing',CreditCard],['Reports','/reports',BarChart3]]},{label:'Communication',items:[['Notifications','/notifications',Bell],['Message Templates','/messages',MessageSquareText]]},{label:'Management',items:[['Stores','/stores',Store],['Settings','/settings',Settings]]}] as const;
const titleMap:Record<string,string>={'/':'Overview','/dashboard':'Overview','/jobs':'Service Jobs','/customers':'Customers','/technicians':'Technicians','/inventory':'Inventory','/billing':'Billing','/reports':'Reports','/notifications':'Notifications','/messages':'Message Templates','/stores':'Stores','/settings':'Settings'};
export function AppShell({children}:{children:React.ReactNode}){const location=useLocation();const navigate=useNavigate();const {jobs,customers,notifications}=useAppStore();const [collapsed,setCollapsed]=useState(false);const [drawer,setDrawer]=useState(false);const [searchOpen,setSearchOpen]=useState(false);const [query,setQuery]=useState('');const [theme,setTheme]=useState<'light'|'dark'|'system'>('light');useEffect(()=>{const fn=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key==='k'){e.preventDefault();setSearchOpen(true)}};window.addEventListener('keydown',fn);return()=>window.removeEventListener('keydown',fn)},[]);useEffect(()=>{const dark=theme==='dark'||(theme==='system'&&window.matchMedia('(prefers-color-scheme: dark)').matches);document.documentElement.classList.toggle('dark',dark)},[theme]);const results=query.length>1?[...jobs.filter(j=>`${j.id} ${j.customer} ${j.phone} ${j.device} ${j.imei}`.toLowerCase().includes(query.toLowerCase())).slice(0,5),...customers.filter(c=>`${c.name} ${c.phone}`.toLowerCase().includes(query.toLowerCase())).slice(0,3)]:[];const path=location.pathname;const pageTitle=path.startsWith('/jobs/')?path==='/jobs/new'?'New Job Card':'Job Details':path.startsWith('/customers/')?'Customer Profile':path.startsWith('/technicians/')?'Technician Workspace':titleMap[path]??'FixFlow';return <div className={cn('app-shell',collapsed&&'sidebar-collapsed')}><aside className={cn('sidebar',drawer&&'sidebar-open')}><div className="brand"><div className="brand-mark"><Smartphone/><span/></div>{!collapsed&&<div><strong>FixFlow</strong><small>Mobile Service Management</small></div>}<button className="mobile-close" onClick={()=>setDrawer(false)} aria-label="Close navigation"><X/></button></div><nav>{groups.map(group=><div className="nav-group" key={group.label}>{!collapsed&&<div className="nav-label">{group.label}</div>}{group.items.map(([label,to,Icon])=><Link key={label} to={to} onClick={()=>setDrawer(false)} className={cn('nav-item',(to==='/'?path==='/'||path==='/dashboard':path.startsWith(to))&&'active')} title={collapsed?label:undefined}><Icon/><span>{label}</span>{label==='Notifications'&&notifications.filter(n=>!n.read).length>0&&<b>{notifications.filter(n=>!n.read).length}</b>}</Link>)}</div>)}</nav><div className="sidebar-foot"><a className="nav-item" href="mailto:support@fixflow.example"><CircleHelp/><span>Help & Support</span></a>{!collapsed&&<div className="user-mini"><div className="avatar">A</div><div><strong>Admin</strong><small><i/> Store Manager</small></div></div>}<button className="collapse-btn" onClick={()=>setCollapsed(!collapsed)} aria-label="Toggle sidebar">{collapsed?<PanelLeftOpen/>:<PanelLeftClose/>}</button></div></aside><div className="workspace"><header className="topbar"><button className="menu-btn" onClick={()=>setDrawer(true)} aria-label="Open navigation"><Menu/></button><div className="topbar-title"><small>FixFlow / {pageTitle}</small><strong>{pageTitle}</strong></div><button className="search-trigger" onClick={()=>setSearchOpen(true)}><Search/><span>Search jobs, customers, IMEI...</span><kbd>⌘ K</kbd></button><div className="header-tools"><div className="theme-switch" aria-label="Theme"><button onClick={()=>setTheme('light')} className={theme==='light'?'selected':''} title="Light"><Sun/></button><button onClick={()=>setTheme('dark')} className={theme==='dark'?'selected':''} title="Dark"><Moon/></button></div><Link to="/notifications" className="icon-btn" aria-label="Notifications"><Bell/><span className="notification-dot"/></Link><button className="store-switch"><Store/><span>ABC Mobile Store</span><ChevronDown/></button><div className="header-user"><div className="avatar">A</div><div><strong>Admin</strong><small>Store Manager</small></div></div></div></header><main className="main-content">{children}</main></div><nav className="mobile-nav">{[{label:'Home',to:'/' as const,Icon:LayoutDashboard},{label:'Jobs',to:'/jobs' as const,Icon:ClipboardList},{label:'Customers',to:'/customers' as const,Icon:Users},{label:'Alerts',to:'/notifications' as const,Icon:Bell},{label:'More',to:'/settings' as const,Icon:Menu}].map(({label,to,Icon})=><Link to={to} key={label} className={path===to?'active':''}><Icon/><span>{label}</span></Link>)}<button className="mobile-fab" onClick={()=>navigate({to:'/jobs/new'})}><Plus/></button></nav><Dialog open={searchOpen} onOpenChange={setSearchOpen}><DialogContent className="search-modal"><DialogTitle className="sr-only">Global Search</DialogTitle><div className="command-input"><Search/><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search Job ID, customer, mobile, IMEI, device..."/><kbd>ESC</kbd></div><div className="command-results">{query.length<2?<div className="search-hint"><Search/><p>Find any repair record instantly</p><span>Try “9876543210” or “iPhone 15 Pro”</span></div>:results.length===0?<div className="search-hint"><p>No matching records</p></div>:results.map((r,i)=>'device'in r?<button key={r.id} onClick={()=>{setSearchOpen(false);navigate({to:'/jobs/$id',params:{id:r.id}})}}><div className="result-icon"><Smartphone/></div><div><strong>{r.id}</strong><span>{r.customer} · {r.device}</span></div><em>{r.status}</em></button>:<button key={r.id} onClick={()=>{setSearchOpen(false);navigate({to:'/customers/$id',params:{id:r.id}})}}><div className="result-icon"><Users/></div><div><strong>{r.name}</strong><span>{r.phone} · Customer</span></div></button>)}</div></DialogContent></Dialog></div>}
