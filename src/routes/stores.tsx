import { createFileRoute } from '@tanstack/react-router';import { Stores } from '@/components/app/management';export const Route=createFileRoute('/stores')({head:()=>({meta:[{title:'Stores — FixFlow'},{name:'description',content:'Multi-store repair operations and performance.'},{property:'og:title',content:'Stores — FixFlow'},{property:'og:description',content:'Multi-store repair operations and performance.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:Stores});
