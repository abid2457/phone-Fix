import { createFileRoute } from '@tanstack/react-router';import { Billing } from '@/components/app/management';export const Route=createFileRoute('/billing')({head:()=>({meta:[{title:'Billing — FixFlow'},{name:'description',content:'Repair invoices, collections, and outstanding payments.'},{property:'og:title',content:'Billing — FixFlow'},{property:'og:description',content:'Repair invoices, collections, and outstanding payments.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:Billing});
