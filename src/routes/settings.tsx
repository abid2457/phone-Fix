import { createFileRoute } from '@tanstack/react-router';import { SettingsPage } from '@/components/app/management';export const Route=createFileRoute('/settings')({head:()=>({meta:[{title:'Settings — FixFlow'},{name:'description',content:'FixFlow business and workspace preferences.'},{property:'og:title',content:'Settings — FixFlow'},{property:'og:description',content:'FixFlow business and workspace preferences.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:SettingsPage});
