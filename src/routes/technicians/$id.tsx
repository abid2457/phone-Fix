import { createFileRoute } from '@tanstack/react-router';import { TechnicianWorkspace } from '@/components/app/management';export const Route=createFileRoute('/technicians/$id')({head:()=>({meta:[{title:'Technician Workspace — FixFlow'},{name:'description',content:'Today’s assigned repairs and technician actions.'},{property:'og:title',content:'Technician Workspace — FixFlow'},{property:'og:description',content:'A focused repair bench workspace.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:Page});function Page(){return <TechnicianWorkspace id={Route.useParams().id}/>}
