import { createFileRoute } from '@tanstack/react-router';import { JobDetails } from '@/components/app/job-details';export const Route=createFileRoute('/jobs/$id')({head:({params})=>({meta:[{title:`${params.id} — FixFlow`},{name:'description',content:'Mobile repair job progress, estimate, payments, and communication.'},{property:'og:title',content:`${params.id} — FixFlow`},{property:'og:description',content:'Complete mobile repair service record.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:Page});function Page(){const {id}=Route.useParams();return <JobDetails id={id}/>}
