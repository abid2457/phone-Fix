import { createFileRoute } from '@tanstack/react-router';import { JobsList } from '@/components/app/jobs-list';export const Route=createFileRoute('/jobs/')({head:()=>({meta:[{title:'Service Jobs — FixFlow'},{name:'description',content:'Search, filter, and manage mobile repair jobs.'},{property:'og:title',content:'Service Jobs — FixFlow'},{property:'og:description',content:'Manage every repair from intake to delivery.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:JobsList});
