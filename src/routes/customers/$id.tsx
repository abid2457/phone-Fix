import { createFileRoute } from '@tanstack/react-router';import { CustomerProfile } from '@/components/app/management';export const Route=createFileRoute('/customers/$id')({head:()=>({meta:[{title:'Customer Profile — FixFlow'},{name:'description',content:'Customer repair, device, payment, and communication history.'},{property:'og:title',content:'Customer Profile — FixFlow'},{property:'og:description',content:'Complete mobile service customer history.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:Page});function Page(){return <CustomerProfile id={Route.useParams().id}/>}
