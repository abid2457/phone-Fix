import { createFileRoute } from '@tanstack/react-router';import { Inventory } from '@/components/app/management';export const Route=createFileRoute('/inventory')({head:()=>({meta:[{title:'Inventory — FixFlow'},{name:'description',content:'Parts stock, value, pricing, and supplier management.'},{property:'og:title',content:'Inventory — FixFlow'},{property:'og:description',content:'Parts stock, value, pricing, and supplier management.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary'}]}),component:Inventory});
