import { createFileRoute } from '@tanstack/react-router';import { Dashboard } from '@/components/app/dashboard';
export const Route=createFileRoute('/')({head:()=>({meta:[{title:'Dashboard — FixFlow'},{name:'description',content:'Today’s mobile repair jobs, revenue, and service center priorities.'},{property:'og:title',content:'FixFlow Service Dashboard'},{property:'og:description',content:'Mobile repair workflow, jobs, revenue, and priorities in one workspace.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:Dashboard});
